Run these commands in preferrably git bash to see the project come to life:

npm init -y

npm install --save three

npm install --save-dev vite

npx vite

Then open your browser to URL: http://localhost:5173/ to see the Flight Sim

Need to have at least Node.JS and npm installed
